import { FullScreenError } from '@components/errors/FullScreenError';

export const NotFoundPage = () => {
  return <FullScreenError title='404' description='Page not found' />;
};
export default NotFoundPage;
